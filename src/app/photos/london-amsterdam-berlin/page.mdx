
import { PhotoJournalLayout } from '@/components/PhotoJournalLayout'

export const article = {
  author: 'Drake Ballew',
  date: '2022-07-10',
  title: 'London, Amsterdam, & Berlin',
  description: 'Description text',
  section: 'Europe',
  content: (
    <div>
      <p>
        Little description
      </p>
    </div>
  ),
}

export const slug = 'london-amsterdam-berlin'

export const metadata = {
  title: article.title,
  description: article.description,
}

export default (props) => <PhotoJournalLayout article={article} slug={slug} {...props} />
