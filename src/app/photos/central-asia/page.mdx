import { PhotoJournalLayout } from '@/components/PhotoJournalLayout'

export const article = {
  author: 'Drake Ballew',
  date: '2013-08-21',
  tripDate: 'Aug - Oct 2013',
  title: 'Central Asia',
  description: "A night spent snowed out on top of a glacier. An epic near-kidnapping hitchhiking experience. Being retained and questioned by the People's Liberation Army at the border. All in one trip!",
  section: 'Asia',
}

export const slug = 'central-asia'

export const metadata = {
  title: article.title,
  description: article.description,
}

export default (props) => <PhotoJournalLayout article={article} slug={slug} {...props} />
