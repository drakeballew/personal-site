import { PhotoJournalLayout } from '@/components/PhotoJournalLayout'

export const article = {
  author: 'Drake Ballew',
  date: '2020-11-15',
  title: 'Kauai, Hawaii',
  description: 'COVID in Kauai',
  section: 'North America',
  content: (
    <div>
      <p>
        Little description
      </p>
    </div>
  ),
}

export const slug = 'kauai-hawaii'

export const metadata = {
  title: article.title,
  description: article.description,
}

export default (props) => <PhotoJournalLayout article={article} slug={slug} {...props} />
