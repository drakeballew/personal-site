import { PhotoJournalLayout } from '@/components/PhotoJournalLayout'

export const article = {
  author: 'Drake Ballew',
  date: '2023-04-20',
  title: 'Paris',
  description: 'Paris with Vaughn',
  section: 'Europe',
  content: (
    <div>
      <p>
        Little description
      </p>
    </div>
  ),
}

export const slug = 'paris'

export const metadata = {
  title: article.title,
  description: article.description,
}

export default (props) => <PhotoJournalLayout article={article} slug={slug} {...props} />
